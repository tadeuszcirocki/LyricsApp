@model LyricsApp.Models.ViewModels.IndexPageModel

<body>
    <form asp-controller="Home" asp-action="GetLyricsByArtistAndTitle" method="get">
        <p>
            <center>
                <label asp-for="data.artist">Artist:</label>
                <input asp-for="data.artist" value="Metallica" />

                <br />

                <label asp-for="data.title">Title:&nbsp;&nbsp;</label>
                <input asp-for="data.title" value="One" />
                <br /><br />
                <input type="submit" value="Get lyrics">
            </center>
        </p>
    </form>
    <div>
        <h3>Your favourite songs:</h3>
        @foreach (var item in Model.favouriteSong)
        {
            <div>
                <h4>
                    @Html.DisplayFor(m => item.Title)
                    by
                    @Html.DisplayFor(m => item.Artist)
                    
                </h4>
                <form asp-action="RemoveFromFavourites">
                    <input asp-for="song.ID" value="@item.ID" type="hidden" />
                    <input type="submit" value="X" />
                </form>
            </div>
        }
    </div>


</body>
